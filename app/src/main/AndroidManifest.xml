<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.keyboard">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".feature.CategoryList.CategoryManageList"></activity>
        <activity android:name=".feature.SignUp.SignUpActivity" />
        <activity android:name=".feature.Category.CategoryManageActivity" />
        <activity android:name=".feature.Item.ItemManageActivity" />
        <activity android:name=".feature.Category.CategoryCreateActivity" />
        <activity android:name=".feature.Item.ItemCreateActivity" />
        <activity android:name=".feature.SignIn.SignInActivity" />
        <activity android:name=".feature.MainActivity" />
        <activity android:name=".feature.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>